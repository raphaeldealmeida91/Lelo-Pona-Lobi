import { Box } from "@mui/material";
import HeaderTitle from "../components/HeaderTitle";

const Actions = () => {
  return (
    <Box>
      <HeaderTitle title="Nos actions" />
    </Box>
  );
};

export default Actions;
