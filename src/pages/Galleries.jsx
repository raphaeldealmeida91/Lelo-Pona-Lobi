import { Box, Typography } from "@mui/material";
import HeaderTitle from "../components/HeaderTitle";

const Galleries = () => {
  return (
    <Box>
      <HeaderTitle title="Galleries" />
    </Box>
  );
};

export default Galleries;
